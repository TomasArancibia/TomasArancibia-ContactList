import React from 'react';
import Contacts from '../components/Contacts';

const ContactList = () => {
    return (
        <div>
            {<Contacts/>}
        </div>
    );
};

export default ContactList;